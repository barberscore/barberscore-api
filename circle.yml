## Customize deployment commands
deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:barberscore.git $CIRCLE_SHA1:master
      - heroku run django-admin migrate --app convention
machine:
  services:
    - elasticsearch
  python:
    version: 2.7.6
